.DS_Store
*.beam
_rel
*.log
_build
relx
.erlang.mk
priv/static/layim
ebin
*bak*
*local*.config
*prod*.config
*runtime*.config
*local*.args
*prod*.args
.imboy.plt
priv/static/layim*
nginx_img.conf
deps
deps.23
deps.24
efmt
logs
.efmt
erldash
*.d
erl_crash.dump
rebar3.crashdump
Mnesia.nonode@nohost
elvis
# Track lock file to ensure reproducible dependency resolution across environments.
# rebar.lock
docker/pg_jieba
docker/timescaledb
priv/ssl
priv/dev_keys/
priv/trial_start.epoch
install_erlang_for_m1.sh
docker/img_fastdfs_data
docker/pgsql15data
docker/imboy_pg15
imboy_v1.sql
# 生产 compose 走单独交付渠道，不随开源仓分发。
# 原模式 `*pro*.yml` 过宽，误伤了 prometheus/promtail/grafana 数据源等无密钥的可观测性配置，
# 导致从远端 clone 的部署方拿不到监控栈配置。
deploy/docker-compose.prod.yml
*pro*.config
*.dev.config
*.dev.args
.pgsync.yml
schema_diff.sql
sys.dev.config
docker/imboy_pg16
relxnode1.config
relxnode2.config
config/vm.node1.args
config/vm.node2.args
docker/imboy_pg17
.idea
merged.sql
chinese-poetry
poetry-source-master
Poetry-master
ssl
.codebuddy
imboy-admin-frontend
settings.json
mcp.json
erl_code
docker-compose-pg17-dev.yml
relxdev.config
docker/imboy_pg18
.cline
/.reports/
.parallel_exec_state/
.task_state/
artifacts/upgrade/
artifacts/mobile_regression
log
artifacts

# Local analysis artifacts
all_exports.txt
export_analysis.txt
temp_exports.txt

# Local AI/tool artifacts
.claude/
.trae/

# Local Codex temp artifacts
.tmp_codex/

# Environment secrets (never commit)
.env
scripts/.env.deploy

# T3.2 split: redocly bundle 产物（CI 临时输出，给 oasdiff / codegen 消费）
# 真源是 api/openapi.yaml + api/components/**；bundled 单文件按需生成。
api/openapi.bundled.yaml
api/openapi.bundled.json

# --- 类型检查工具产物（ELP/Gradualizer，不入仓） ---
/.elp/
/.gradualizer/
/tools/

# B-25：Alertmanager 通知渠道取值（收件邮箱 / webhook URL）。
# webhook URL 本身就是凭据，谁拿到谁就能往群里发消息 —— 绝不入库。
# 仓库里只保留 alertmanager.env.example（全部留空）与模板。
deploy/alertmanager/alertmanager.env
