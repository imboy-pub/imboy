-- ============================================================
-- 回滚 000016: 移除 msg_c2g 主表 msg_id 幂等唯一索引
--   回滚后 msg_c2g 主表恢复无 msg_id 唯一约束，repo 层 ON CONFLICT 将因
--   缺少匹配唯一索引而报 PG 42P10，需同步回滚 repo 代码改动。
-- ============================================================

DROP INDEX IF EXISTS public.uk_c2g_msgid_createdat;
