%% IMBoy Plugin Manifest — channel
%% 频道 (Channel) 订阅与邀请系统
%% 由 imboy_plugin_loader 启动期加载至 persistent_term:{imboy_plugin_manifest, channel}
%% Source of truth: doc/plugin/contract.md §3 / §4.2
%% Mirror of: src/lib/imboy_plugin_registry.erl#raw_manifests/0 (channel)
#{
    name => channel,
    version => <<"1.0.0">>,
    contract_version => {1, 0},
    kind => plugin,
    description => <<"频道 (Channel) 订阅与邀请系统"/utf8>>,
    depends_on => #{},
    requires_capabilities => [],
    min_core_version => <<"1.0.0-rc.2">>,
    children => [],
    routes => [],
    migrations => #{
        dir => <<"priv/migrations">>,
        table_prefix => <<"channel_">>,
        preserve_on_uninstall => true
    },
    features => #{
        channel => #{
            default => true,
            description => <<"频道基础订阅"/utf8>>,
            rollout => always,
            audience => #{kind => all}
        },
        channel_discover => #{
            default => true,
            description => <<"频道发现"/utf8>>,
            rollout => always,
            audience => #{kind => all}
        },
        channel_invitation => #{
            default => true,
            description => <<"频道邀请"/utf8>>,
            rollout => always,
            audience => #{kind => all}
        },
        channel_order => #{
            default => false,
            description => <<"频道付费订阅（实验性）"/utf8>>,
            rollout => always,
            audience => #{kind => all}
        }
    },
    limits => #{
        rps_per_uid => 0,
        max_concurrent_calls => 0,
        msg_quota_per_day => 0
    },
    budget => #{
        max_connections => 0,
        max_subscriptions => 0,
        mem_mb_soft_limit => 256
    },
    degrade => #{on_unhealthy => route_404},
    circuit_breaker => #{
        enabled => false,
        threshold_pct => 50,
        window_sec => 60
    },
    entries => #{
        app => [channel_tab, channel_discover_page],
        admin => [channels_page]
    },
    i18n => #{keys => []},
    subscribes => [],
    publishes => [],
    publishes_meta => #{},
    audit => #{events => []},
    meta => #{
        author => <<"imboy-pub">>,
        license => <<"Apache-2.0">>,
        created_at => <<"2026-04-29">>,
        legacy_handlers => [channel_handler, report_handler]
    }
}.
