# E2EE-000 Evidence — 可复现基线与证据目录

- **Task**: E2EE-000（只读核查 + 证据目录建立；不修改产品代码）
- **Date**: 2026-07-20
- **Nature**: read-only baseline
- **Decision**: PASS

## 1. 仓库基线（Repositories before/after）

| 仓库 | toplevel | HEAD | branch | 工作树 | 与远端 |
|---|---|---|---|---|---|
| imboy | `/Users/leeyi/project/imboy.pub/imboy` | `124920c14d8928860f9b1ec9c6af59a870062b74` | main | clean | 领先 upstream **11** 提交（未 push） |
| imboyapp | `/Users/leeyi/project/imboy.pub/imboyapp` | `5d5d06c9351ae887fd881b6ad826a953a0df1b21` | main | 仅未跟踪 `.codex/`、`AGENTS.md`（与本任务无关，不改动） | 无 upstream |

> `imboy.pub` 根目录非 Git 仓库（umbrella workspace），符合 CLAUDE.md「关键结构事实」。

## 2. ADR 签字状态（决定后续 Batch 走向）

`rg -n '状态.*Proposed' docs/guides/e2ee/v2/1[4-9]-*.md`：

- ADR 14/15/16/17/18/19 **全部仍为「状态：Proposed」**。
- 结论：**E2EE-001（人工签字门）未满足 → BLOCKED**。执行者不得代签。
- 依据 ADR 20 header 与 playbook §1.6 停止条件 #1：未签字前，凡改变 wire/schema/trust model 的任务（E2EE-012/020+、cross-signing、Recovery v2、MLS）保持 BLOCKED；仅「已确认 P0 漏洞修复」可另获用户明确授权执行（E2EE-010/011/013/015/016）。

## 3. Gap 现状重定位（rg 实测，不照抄旧 handoff）

| Gap（ADR 20 §1.2） | 当前落点与证据 | 状态 |
|---|---|---|
| G1 C2C 默认走 Megolm / Olm 开关关闭 | `imboyapp/lib/page/chat/chat/services/chat_network_service.dart:562` `static const bool useOlmForC2C = false;`；:519-534 发送路径统一 Megolm | 存在 |
| G2 room-key 收端 Olm 失败可回 RSA | `imboyapp/lib/service/group_session_service.dart:327-330` 接收状态机；测试证明「olm 缺 sid → 回退 RSA」「Olm 会话不可用 → 回退 RSA」仍在；「Olm 认证失败 → 拒绝」已硬化（ADR13 部分完成） | 部分存在（Strict 降级面未闭） |
| G3 外层路由/展示字段未统一认证 | `imboyapp/lib/service/e2ee_service.dart:428-431` `sender_did` 从 payload 注入 plain | 存在 |
| G4 登录 token 未强绑定 DID，body DID 可参与授权 | `imboy/src/api/olm_handler.erl:79/113/145` `device_id` 取自 PostVals（body）；`src/ds/auth_ds.erl:55` DID 取自 header | 存在 |
| G5 strict/compliance 取钥失败可继续 | `imboyapp/lib/service/compliance_key_service.dart:70` `catch(e)` 兜底路径（81 行文件） | 存在 |
| G6 logout 未完整清除 E2EE/DB keys | `imboyapp/lib/store/repository/user_repo_local.dart`(270)、`lib/service/storage_secure.dart`(413) | 待 E2EE-015 核 |
| G7 备份仅覆盖 RSA / parser 边界不足 | `imboyapp/lib/service/e2ee_local_backup_service.dart`(610) | 待 E2EE-016 核 |
| G8 trust event freshness/唯一性不足 | `imboy/src/logic/e2ee_trust_logic.erl` 存在；migration `00000044_device_trust` | 待 E2EE-014 核 |
| G9 OTK 仅粗粒度限流 | migration `00000045_olm_otk_audit` 已在；claim handler `olm_handler.erl:218-237` | 待 E2EE-025 核 |

最新迁移序号：`00000046_compliance_key_drop_private`（E2EE-014 将用 `00000047`）。

## 4. 基线命令与结果

| 命令 | 环境 | 结果 |
|---|---|---|
| `make compile`（imboy） | 本机 OTP28 | exit 0 |
| `dart analyze lib`（imboyapp） | 本机 Flutter | `No issues found!` exit 0 |
| `flutter test test/service/e2ee/ test/service/group_session_service_test.dart` | 模拟器/无头（仅调试基线，非真机验收） | **+62 ~10 skip 0 fail**，All tests passed |

既有 E2EE 测试模块（imboy `test/`）：`e2ee_logic_tests`、`e2ee_trust_logic_tests`、`olm_identity_*`、`user_device_*`、`elib_cipher_e2ee_v2_tests`、`e2ee_*_integration_tests` 等 30+ 个（beam 已编译，本次未跑全量 eunit）。

> 10 个 skip 集中在需真机/环境的用例；真机验收留待各任务 Gate。全量 `make eunit` 与真机矩阵不在 E2EE-000 只读基线范围，按各任务卡执行。

## 5. Gap↔测试绑定（每条 Gap 至少一个测试文件）

| Gap | 现有/待新增测试 |
|---|---|
| G1 | 待新增 `test/service/e2ee/` C2C Olm fan-out（E2EE-024）；现 `e2ee_outbound_router_test.dart` |
| G2 | `test/service/group_session_service_test.dart`（现覆盖 ADR13，E2EE-011 扩「Strict 拒绝降级」） |
| G3 | 待新增 PFv3 mutation matrix（E2EE-012/021） |
| G4 | 待新增 `imboy/test` device-bound auth（E2EE-013）；现 `user_device_*_tests` |
| G5 | 待新增 compliance fail-closed（E2EE-010） |
| G6 | 待新增 logout 清理 canary（E2EE-015） |
| G7 | `e2ee_local_backup_repo_tests` + 待新增 fuzz（E2EE-016） |
| G8 | `e2ee_trust_logic_tests`（E2EE-014 扩 freshness/幂等） |
| G9 | `olm_otk_cleanup_worker_tests` + 待新增并发耗尽（E2EE-025） |

## 6. Secrets/Log scan

- 本证据不含 token、手机号、生产地址、密钥或消息内容。
- 除本文件外无产品代码变更（imboyapp 未跟踪文件为 `.codex/`、`AGENTS.md`，非本任务产出）。

## 7. Residual risks / 停止点

- ADR 14–19 未签字 → E2EE-001 BLOCKED，pipeline 在 B00 停止。
- 后续仅在用户明确授权「作为已确认 P0 漏洞修复」的前提下方可进入 B01（E2EE-010/011）。
- Git commit 身份需当前会话人工确认（本任务未提交，无需身份）。

- **Reviewer**: 待安全 reviewer 复核（基线只读，无安全 diff）
- **Decision**: PASS
