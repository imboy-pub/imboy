# E2EE-027 Evidence — Atomic Outbox/Inbox and Crash Recovery

- **Task**: E2EE-027 — Outbox、dedupe 和 crash recovery
- **Date**: 2026-07-27
- **Repository**: imboyapp
- **Before HEAD**: 7912e001 feat(e2ee): implement DeviceManifest and Signed Capabilities negotiation
- **After HEAD**: 7912e001 (not committed)
- **ADR clauses**: ADR 14 §4.2 Invariant 7 (crypto state updates must be atomic with message state); ADR 20 §9 S2.3 (Transactional CryptoStore)
- **Changed files**:
  - `imboyapp/lib/service/e2ee/crypto_store.dart` — 新增 `insertOutbox` 独立 outbox 写入方法（ON CONFLICT 幂等）
  - `imboyapp/lib/service/e2ee/e2ee_outbound_router.dart` — `encryptV3` 加密后原子写入 immutable outbox
- **Tests added first and old behavior reproduced**:
  - `imboyapp/test/service/e2ee/outbox_crash_recovery_test.dart` — 9 个新测试，覆盖 outbox 集成、崩溃重发、inbox dedupe 原子性
  - 首次运行 4/9 失败（outbox 未集成）→ 实现后 9/9 通过
- **Verification commands**:
  - `flutter test test/service/e2ee/outbox_crash_recovery_test.dart`
  - `flutter test test/service/e2ee/`
  - `dart analyze lib/service/e2ee/e2ee_outbound_router.dart lib/service/e2ee/crypto_store.dart`
- **Verification result/count/skip count**:
  - outbox_crash_recovery_test.dart: 9 passed, 0 failed, 0 skipped
  - Full E2EE suite: 283 passed, 0 failed, 0 skipped
  - dart analyze: No issues found
- **Real device / PostgreSQL environment**: N/A (SQLite ffi in-memory 测试覆盖；真机验证在 B13 GA gate 执行)
- **Security negative cases**:
  - encryptV3 加密后 outbox 写入的 payload 为完整 v3 信封（只含密文和 header hash，不含明文/私钥）
  - 重发从 outbox 读取复用 ciphertext，不重新调用 encrypt → 零 key reuse
  - dedupeAndPersistSession 重复消息回滚事务，ratchet 不重复推进
  - dedupe insert 失败时 session persist 随事务一起回滚（原子性）
- **Secrets/log scan**: outbox payload 只含 base64url ciphertext + header_hash，无明文/私钥/log 泄漏
- **Migration and rollback result**: N/A（无 schema migration，仅新增方法）
- **Residual risks**:
  - outbox 写入在 `SqliteService.to.db` 不可用时降级为非事务化（不阻塞发送，但失去崩溃恢复能力）
  - 真机 crash consistency 测试（10,000 次 kill/restart 注入）在 B20 E2EE-051 执行
- **Reviewer**: Pending
- **Decision**: PASS
