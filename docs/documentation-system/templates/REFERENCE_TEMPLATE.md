# [主题] 参考

> **类型**：参考 · **读者**：[角色] · **适用版本**：≥ vX.Y · **最后验证**：YYYY-MM-DD

<!-- 参考文档只陈述事实：参数、行为、默认值、错误。不教学、不劝诫。完整 > 精炼。 -->

## 概述

[一段话说明本参考覆盖的范围。有前置概念则链接到解释型文档，不在这里展开。]

## [实体/接口/配置] 明细

### `name_of_thing`

[一句话说明用途]

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `param_a` | string | 是 | — | [含义、取值范围、格式示例] |
| `param_b` | number | 否 | `5000` | [含义] |

**示例**：

```bash
# 请求 / 调用示例
```

```json
// 响应 / 返回示例
```

### `name_of_next_thing`

……

## 错误码

| 错误码 | HTTP 状态 | 含义 | 处理建议 |
|--------|-----------|------|---------|
| `EXAMPLE_ERROR` | 400 | [触发条件] | [调用方该怎么做] |

## 边界与限制

<!-- 速率限制、分页上限、字段最大长度等——这些最容易被遗漏，单独成节 -->

- [限制 1：如"单页最大 100 条"]
- [限制 2]

## 版本变化

| 版本 | 变化 |
|------|------|
| v2.3.0 | [新增/废弃了什么] |
