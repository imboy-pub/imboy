# E2E-01 — 全本地 Golden Flow Harness 与证据包

> 所属主计划：[IMBoy Agent Hub 产品收敛执行计划](../ai-agent-mcp-bot-webhook-product-convergence-plan-2026-09.md)
> 本文件是 E2E-01 的权威执行卡。跨任务冻结决策、全局协议、依赖拓扑和独占边界以主计划 §1-§7 为准。

执行前必须先完成：

- 执行主计划 §4.1 的路径探测，使用 `$IMBOY_WORKSPACE_ROOT` 与 `$IMBOY_EVIDENCE_ROOT`，不得写死个人路径。
- 核对依赖状态和 §7 独占边界；只修改本卡声明的范围。
- 后端测试模块名以 G0-01 的 `test-module-map.tsv` 为准；卡片计划名变化时由 Coordinator 同步。
- 证据写入 `$IMBOY_EVIDENCE_ROOT/E2E-01/evidence.json` 并经 EVID-00 verifier 判定。
- 不 reset/clean/stash，不覆盖共享改动；未经人工确认不 commit，不 push，不执行外部或生产动作。

- Status：`blocked`
- Owner：E2E-Evidence
- Dependencies：BUILD-01、EVID-00、TRACE-00
- Repo：`imboy`，只新增 harness/fixture/runbook，复用 EVID-00 verifier。
- Actions：
  1. 启动 scratch PG、后端、fake LLM provider、fake Bot webhook receiver 和最小 MCP client。
  2. 生成两个用户、一个非 E2EE 群、一个 Agent、一个 Bot、一个 MCP Client；所有 secret 只存在临时目录。
  3. 跑必选 Golden Flow：incoming webhook -> 内建 Agent 纯对话/分析 -> 外部 MCP Agent task/progress/HITL -> human approve -> Bot mention/delivery/reply。若 `AGT-01=Go`，另跑内建 Agent 受控 tool 创建任务的可选支路；No-Go 时不得调用该支路。
  4. 运行负例：MCP revoked/expired、tool no grant、重复 idempotency key、Webhook 5xx/retry/dead/replay、跨群 Bot reply、E2EE task、服务重启恢复。
  5. 输出统一 evidence.json 和 sha256 manifest；清理只删除本 harness 创建且有 marker 的临时资源。
  6. 产出 `docs/operations/agent-hub-local-golden-flow.md`：从空 scratch 环境复现安装、配置、启动、验证、清理、证据归档，以及 WH-01 AEAD 主密钥轮换/丢失恢复演练。
  7. 此卡第一次在功能合并后执行；Coordinator 完成所有集成/cherry-pick 后必须重新分派同一 E2E-01 owner 在最终 Base 重跑，并用新证据原子替换旧证据。旧 Base 证据只能标 `superseded`。
- Verify：

```bash
cd "$IMBOY_WORKSPACE_ROOT/imboy"
bash scripts/agent_hub_golden_flow.sh --profile local-fixture --evidence-dir "$IMBOY_EVIDENCE_ROOT/E2E-01"
python3 scripts/verify_agent_hub_task_evidence.py --task "$IMBOY_EVIDENCE_ROOT/E2E-01/evidence.json"
shasum -a 256 "$IMBOY_EVIDENCE_ROOT"/E2E-01/*
```

- Acceptance IDs：`E2E-01-A01` Golden Flow 可按 correlation_id 重建 request/task/event/approval/execution/delivery/outcome；`E2E-01-A02` 正例全 PASS 且负例按预期拒绝；`E2E-01-A03` 重启不丢审批/交付/correlation；`E2E-01-A04` 日志扫描无 secret/PII；`E2E-01-A05` 清理后无残留进程和 scratch DB；`E2E-01-A06` runbook 在空 scratch 环境复现并覆盖 AEAD 主密钥生命周期；`E2E-01-A07` 最终合并 Base 重跑证据通过且旧证据 superseded。
- Stop：脚本不得访问非 loopback 地址；任何 URL 解析为公网/局域网即退出。
