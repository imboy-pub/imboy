# WH-01 — Bot 凭证安全与出站 Webhook 可靠交付

> 所属主计划：[IMBoy Agent Hub 产品收敛执行计划](../ai-agent-mcp-bot-webhook-product-convergence-plan-2026-09.md)
> 本文件是 WH-01 的权威执行卡。跨任务冻结决策、全局协议、依赖拓扑和独占边界以主计划 §1-§7 为准。

执行前必须先完成：

- 执行主计划 §4.1 的路径探测，使用 `$IMBOY_WORKSPACE_ROOT` 与 `$IMBOY_EVIDENCE_ROOT`，不得写死个人路径。
- 核对依赖状态和 §7 独占边界；只修改本卡声明的范围。
- 后端测试模块名以 G0-01 的 `test-module-map.tsv` 为准；卡片计划名变化时由 Coordinator 同步。
- 证据写入 `$IMBOY_EVIDENCE_ROOT/WH-01/evidence.json` 并经 EVID-00 verifier 判定。
- 不 reset/clean/stash，不覆盖共享改动；未经人工确认不 commit，不 push，不执行外部或生产动作。

- Status：`blocked`
- Owner：Backend-Webhook-Outbound
- Dependencies：PDT-01、TRACE-00
- Repo：`imboy`
- Files：migration 92；Bot credential repo/ds/auth 路径；新 delivery repo/ds/worker；`bot_webhook_logic.erl`；tests。
- Actions：
  1. `api_token` 改为高熵 token 摘要 + prefix 查询，明文只在创建/轮换时返回一次；旧明文 token 有明确迁移截止路径。
  2. `verify_token` 因发送签名必须可取回，复用仓内 AEAD/私密配置范式加密存储；加密主密钥缺失或解密失败时 fail-closed，不降级读明文。
  3. 主消息路径只写 outbox，不直接依赖外部 HTTP 成功；写入需有唯一 delivery_id/idempotency key，并继承 TRACE-00 correlation_id。
  4. worker 执行有界重试，例如 `0s/5s/30s/5m` 后进入 dead；精确次数以 PDT-01 契约为准。
  5. 签名覆盖 timestamp、delivery_id、raw body；使用常量时间验证示例；支持当前/上一把 verify secret 的短轮换窗。
  6. URL 默认仅允许 HTTPS 公网目标；内网目标必须命中部署侧精确 egress allowlist，loopback 只可在 test profile 显式开放；metadata/link-local 永远拒绝。
  7. 注册时与发送前都重新解析和校验；实际连接 pin 校验后的 IP 并保留 Host/SNI；限制 redirect，并逐跳重新解析、校验和 pin，防 DNS rebinding/重定向进入禁区。
  8. 记录 attempt、HTTP status class、latency、next_retry_at 和截断错误；不存响应正文和 secret。
  9. 提供管理员单次重放 dead delivery 的 logic/handler，重放生成新的 attempt 但不改变原 delivery_id。
  10. 在 E2E-01 runbook 写明 AEAD 主密钥来源、双密钥轮换、旧密钥退役、丢失时 fail-closed 和批量轮换 Bot verify secret 的恢复步骤。
- Verify：

```bash
cd "$IMBOY_WORKSPACE_ROOT/imboy"
make app
make eunit-local t=bot_webhook_delivery_repo_tests
make eunit-local t=bot_webhook_delivery_worker_tests
make eunit-local t=bot_webhook_logic_tests
make eunit-local t=bot_ds_tests
make eunit-local t=bot_handler_tests
make eunit-local t=bot_e2e_tests
git diff --check
```

- Acceptance IDs：`WH-01-A01` Bot API token 库中零明文且 verify secret 仅以可认证密文存储/轮换；`WH-01-A02` 外部服务慢/挂不阻塞消息 ACK；`WH-01-A03` 2xx 成功、5xx/timeout 有界重试、4xx 不无限重试；`WH-01-A04` 死信可审计和手工重放；`WH-01-A05` SSRF/redirect/DNS rebinding 负例全拒绝且连接使用 pinned IP；`WH-01-A06` allowlist 内网正例与独立签名 fixture 通过；`WH-01-A07` 主密钥轮换/丢失恢复步骤可演练。
- Stop：需要真实公网域名或第三方 webhook 时使用本地 fixture，仍无法验证则 `blocked_external`。
