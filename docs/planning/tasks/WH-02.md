# WH-02 — Channel Incoming Webhook 凭证摘要与轮换

> 所属主计划：[IMBoy Agent Hub 产品收敛执行计划](../ai-agent-mcp-bot-webhook-product-convergence-plan-2026-09.md)
> 本文件是 WH-02 的权威执行卡。跨任务冻结决策、全局协议、依赖拓扑和独占边界以主计划 §1-§7 为准。

执行前必须先完成：

- 执行主计划 §4.1 的路径探测，使用 `$IMBOY_WORKSPACE_ROOT` 与 `$IMBOY_EVIDENCE_ROOT`，不得写死个人路径。
- 核对依赖状态和 §7 独占边界；只修改本卡声明的范围。
- 后端测试模块名以 G0-01 的 `test-module-map.tsv` 为准；卡片计划名变化时由 Coordinator 同步。
- 证据写入 `$IMBOY_EVIDENCE_ROOT/WH-02/evidence.json` 并经 EVID-00 verifier 判定。
- 不 reset/clean/stash，不覆盖共享改动；未经人工确认不 commit，不 push，不执行外部或生产动作。

- Status：`blocked`
- Owner：Backend-Webhook-Inbound
- Dependencies：PDT-01、TRACE-00
- Repo：`imboy`
- Files：migration 93；`channel_webhook_repo/ds/logic/handler`；tests。
- Actions：
  1. 新 token 只返回一次；存 digest + prefix，不存可还原明文。
  2. 迁移期为旧 token 提供有截止条件的双读/回填策略；不得在日志打印旧 token。
  3. 增加 rotate：新 token 生效后旧 token只在短 grace window 有效，随后稳定 404。
  4. list 只显示 prefix/status/last_used_at；disable/rotate 必须频道管理员且 channel locked。
  5. 保留匿名入口 body limit 和 IP/token 双维限流；无效 token 不泄露存在性；成功请求创建/继承可信 correlation_id。
- Verify：

```bash
cd "$IMBOY_WORKSPACE_ROOT/imboy"
make app
make eunit-local t=channel_webhook_repo_tests
make eunit-local t=channel_webhook_ds_tests
make eunit-local t=channel_webhook_logic_tests
make eunit-local t=channel_webhook_handler_tests
make eunit-local t=auth_middleware_api_v1_tests
git diff --check
```

- Acceptance IDs：`WH-02-A01` 新 token 库中零明文；`WH-02-A02` rotate/grace/disable 时间边界可测；`WH-02-A03` 跨频道和非管理员拒绝；`WH-02-A04` 无效、过期、停用均统一外部响应；`WH-02-A05` migration up/down 不丢旧记录且成功事件保留 correlation。
