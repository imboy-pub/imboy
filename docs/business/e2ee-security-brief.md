# imboy 端到端加密（E2EE）安全摘要

> 面向：企业 IT 决策者 / 安全审计方
> 版本：2026-07 | 产品等级：Preview（私有化部署）

---

## 一句话定位

imboy 是**服务端零密码学**的企业私有化即时通讯平台——服务器全程不接触明文消息和私钥，即使服务器被完全攻破，攻击者也无法解密任何历史消息。

---

## 核心安全属性

| 属性 | 含义 | 对标 |
|------|------|------|
| 端到端加密 | 消息在发送方设备加密、接收方设备解密，服务端仅转发密文 | Signal / Matrix |
| 前向保密 (PFS) | 长期密钥泄露不影响历史消息安全（Double Ratchet） | Signal Protocol §3 |
| 攻破后愈合 (PCS) | 会话被攻破后，下一次 DH 交换即自动愈合 | Signal Protocol §4 |
| 降级防护 | 高水位标记 (HWM) 阻止中间人强制回退到弱协议 | — |
| 篡改可检测 | SHA-256 哈希链审计日志，任何历史修改即刻暴露 | Certificate Transparency 思路 |
| 身份认证 | Ed25519 签名 + TOFU 固定 + 安全码（60 位指纹）比对 | Signal Fingerprint v2 |

---

## 服务端零知识保证（7 条不变量）

1. 不存储明文私钥——私钥从未离开用户设备
2. 不解密消息——密文作为不透明字节流路由
3. OTK 原子消费——一次性预密钥 FIFO + 行锁，防重放
4. CI 密码学门禁——任何服务端解密代码引入即刻构建失败
5. 推送不泄密——离线推送仅含"发来一条消息"，密文不入推送通道
6. 错误不泄密——API 错误返回不透明 `internal_error`，数据库 schema 不外露
7. 群消息透传——C2G 加密字段全链路 byte-for-byte 不修改

---

## 可验证性（独立审计）

| 维度 | 数据 |
|------|------|
| 自动化安全测试 | **509 个**（客户端 244 + 后端 265），零跳过 |
| 威胁模型覆盖 | 12 类威胁 × 对应守护测试，100% 覆盖 |
| 一键复现 | 后端 `make e2ee-verify` / 客户端 `bash scripts/run_e2ee_suite.sh` |
| 协议规范 | `docs/reference/e2ee-protocol-specification.md` v1.1.0（英文，供审计方阅读） |
| 密码学依赖 | 全部委托 vodozemac (Matrix.org Rust 库) + SQLCipher，无自研原语 |

审计方只需 clone 仓库、执行两条命令，即可在**无服务器、无数据库、无真机**环境下复现全部 509 个安全断言。

---

## ⚠️ 合规密钥托管：必读披露

**如果贵司启用 `compliance` 模式，消息将不再是纯端到端的。** 这一节我们主动写明，
而不是等尽调时被问出来。

### 机制

`e2ee_mode` 有四个取值，**出厂默认是 `disabled`**，合规托管**必须由部署方显式开启**：

| `e2ee_mode` | 谁能读到明文 |
|---|---|
| `disabled` | E2EE 关闭，不适用 |
| `optional` | 同上（当前实现等价于关闭，见下方说明） |
| `required` | **仅收发双方设备**。纯端到端，无第三方通道 |
| `compliance` | 收发双方设备 **+ 持有合规私钥的审计方** |

在 `compliance` 模式下，客户端加密每条消息时，除了用各接收设备的公钥 wrap 一份 AES 会话密钥，
**还会额外用一枚"合规公钥"再 wrap 一份**，作为 `did: compliance-audit` 条目写入消息。
持有对应合规私钥的人，因此可以解密该模式下的**全部**消息。

### 这与"服务端零知识"是否矛盾

**不矛盾，但你需要理解边界**：

- 合规私钥由**审计方在本地（HSM / 离线介质）自行生成**，**永不上传服务端**。
  服务端只接收公钥（`compliance_key` 表已 `DROP COLUMN private_key_encrypted`）。
- 因此**服务器被完全攻破，攻击者仍拿不到合规私钥**，也就解不开消息 —— 零知识宣称成立。
- 但**持有合规私钥的那个人（组织内的审计员）能读全部消息**。
  纯端到端语义在这个模式下确实被打破了，这是设计意图，不是缺陷。

### 部署方的责任

1. **在隐私政策中向最终用户披露**该托管机制 —— 这是部署方的法定义务，不是我方代办事项。
2. **指定合规私钥的保管责任人、离线介质存放位置与访问审计流程。**
3. ⚠️ **私钥丢失不可恢复**：服务端不存任何私钥副本，合规私钥一旦丢失，
   用该公钥加密过的全部历史密文**永久无法通过合规通道解密**。
4. **不需要合规托管就别开**：配置 `required` 即为纯端到端，不产生第二收件人。

> `optional` 的已知设计债务：客户端本地开关目前硬编码为关闭，该模式实际等价于不加密。
> 需要加密请直接用 `required` 或 `compliance`。

> 技术细节与代码锚点见 [`docs/compliance/e2ee-policy.md`](../compliance/e2ee-policy.md) §3。
> 本节陈述技术事实；对外法律措辞请贵司法务据此自行审定。

---

## 与主流企业 IM 对比

| 能力 | imboy | 钉钉/飞书/企微 | Matrix (Element) |
|------|-------|--------------|-----------------|
| 真 E2EE（服务端零知识） | 是 | 否（TLS 传输加密，服务端可读） | 是 |
| 私有化部署 | 是（Docker Compose 单节点） | 部分（需商务定制） | 是 |
| PFS + PCS | 是（Double Ratchet） | 不适用 | 是 |
| 降级防护 | 是（HWM + fail-closed） | 不适用 | 部分 |
| 合规密钥托管 | 可选（`compliance` 模式双 wrap，**默认关闭**，见上节披露） | 服务端全权 | 否 |
| 一键安全审计 | 是（509 tests 可复现） | 不公开 | 需自行搭建 |

---

## 部署形态

- **Docker Compose（单节点）** —— **当前唯一受支持的交付形态**：含 Prometheus + Grafana + Loki 可观测性栈
- **Helm Chart（Kubernetes）** —— **实验性，未经生产集群验证，不在交付与支持范围内**。
  集群水平扩展为路线图项。
- 数据主权：全部数据（PostgreSQL + Garage S3 附件）留在客户机房

---

## 下一步

- 预约 30 分钟在线演示（含真机 E2EE 消息收发 + 安全码比对）
- 获取协议规范文档进行内部安全评审
- 讨论 PoC 部署方案（1 周内可交付可试用环境）

---

*imboy — 企业私有化即时通讯，消息主权归你。*
