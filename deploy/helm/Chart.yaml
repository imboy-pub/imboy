apiVersion: v2
name: imboy
description: IMBoy IM 平台 —— Erlang/OTP 后端 + React 管理前端
type: application
version: 0.1.0
appVersion: "1.0.0-rc.1"

keywords:
  - imboy
  - im
  - erlang
  - messaging

maintainers:
  - name: IMBoy Team
    email: ops@example.com

dependencies: []
# 注意：PostgreSQL 不在此 chart 中管理，假设使用外部 managed DB
# (AWS RDS / GCP Cloud SQL / 自建 PG operator 均可)
