# GET /api/adm/group/file/list — admin
get:
  tags: [admin]
  summary: 管理后台群文件列表
  description: 分页获取指定群的文件列表，支持分类和关键词筛选（管理员视角）。List group files with pagination, category and keyword filtering (admin view).
  operationId: adm_group_file_list
  parameters:
    - in: query
      name: gid
      required: true
      schema: { type: integer }
      description: 群 ID
    - in: query
      name: category
      required: false
      schema: { type: string }
      description: 文件分类
    - in: query
      name: keyword
      required: false
      schema: { type: string }
      description: 搜索关键词
    - $ref: '../../components/parameters/PaginationPage.yaml'
    - $ref: '../../components/parameters/PaginationSize.yaml'
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    properties:
                      list:
                        type: array
                        items: { type: object, additionalProperties: true, description: '记录对象（字段以对应表/handler 为准）' }
                      total: { type: integer }
                      page: { type: integer }
                      size: { type: integer }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
