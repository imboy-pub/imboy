# POST /adm/group/file/delete — admin
post:
  tags: [admin]
  summary: 管理后台删除群文件
  operationId: adm_group_file_delete
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required: [file_id]
          properties:
            file_id: { type: integer, description: '目标 ID' }
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
