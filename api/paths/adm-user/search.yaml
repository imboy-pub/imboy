# GET /adm/user/search — admin
get:
  tags: [admin]
  summary: admin adm-user - search
  description: |
    ## 中文 / English
    管理后台 adm-user handler 的 `search` 动作；路径 `/adm/user/search`。
    Admin handler `adm-user`, action `search`. Path `/adm/user/search`.
  operationId: adm_user_search
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
