# DELETE /api/adm/app_ddl/delete — admin
delete:
  tags: [admin]
  summary: admin adm-app-ddl - delete
  description: |
    ## 中文 / English
    管理后台 adm-app-ddl handler 的 `delete` 动作；路径 `/adm/app_ddl/delete`。
    Admin handler `adm-app-ddl`, action `delete`. Path `/adm/app_ddl/delete`.
  operationId: adm_app_ddl_delete
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required: [id]
          properties:
            id: { type: integer, description: '要删除的 DDL 记录 ID / DDL record ID to delete' }
  responses:
    '200':
      description: 成功（返回提交的 PostVals）
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    additionalProperties: true
                    description: 删除成功时返回原始提交参数
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
