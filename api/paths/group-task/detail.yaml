# GET /v1/group/task/detail
get:
  tags: [user]
  summary: 任务详情
  description: |
    ## 中文 / English
    任务详情
    Task detail
  operationId: detailGroupTask
  parameters:
    - in: query
      name: task_id
      required: true
      schema: { type: integer, minimum: 1 }
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 参数非法 / 无权限 / 不存在
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
