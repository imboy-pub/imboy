# POST /adm/group/vote/close — admin
post:
  tags: [admin]
  summary: 管理后台关闭群投票
  description: 管理员强制关闭指定群投票（提前终止投票）。Force close a group vote by admin (ends voting early).
  operationId: adm_group_vote_close
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required: [vote_id]
          properties:
            vote_id: { type: string, description: '投票 ID' }
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
