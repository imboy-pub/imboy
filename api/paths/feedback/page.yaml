# GET /feedback/page
get:
  tags: [user]
  summary: 反馈列表（自己 / admin）
  description: |
    ## 中文 / English
    反馈列表（自己 / admin）
  operationId: feedbackPage
  parameters:
    - $ref: '../../components/parameters/PaginationPage.yaml'
    - $ref: '../../components/parameters/PaginationSize.yaml'
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
