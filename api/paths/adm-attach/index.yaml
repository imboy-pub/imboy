# GET /api/adm/storage/index — admin
get:
  tags: [admin]
  summary: admin adm-attach - index
  description: |
    ## 中文 / English
    管理后台 adm-attach handler 的 `index` 动作；路径 `/adm/storage/index`。
    Admin handler `adm-attach`, action `index`. Path `/adm/storage/index`.
  operationId: adm_attach_index
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    properties:
                      list:
                        type: array
                        description: 附件列表
                        items:
                          type: object
                          additionalProperties: true
                      total:
                        type: integer
                      page:
                        type: integer
                      size:
                        type: integer
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
