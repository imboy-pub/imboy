# POST /api/adm/attach/auth — admin
post:
  tags: [admin]
  summary: admin adm-attach - auth
  description: |
    ## 中文 / English
    管理后台 adm-attach handler 的 `auth` 动作；路径 `/adm/attach/auth`。
    Admin handler `adm-attach`, action `auth`. Path `/adm/attach/auth`.
  operationId: adm_attach_auth
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    properties:
                      uri:
                        type: array
                        description: 各 URI 授权检查结果列表
                        items:
                          type: object
                          additionalProperties: true
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
