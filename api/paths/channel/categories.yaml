# GET (channel) channelCategories
get:
  tags: [user]
  summary: 频道分类列表
  description: |
    ## 中文 / English
    频道分类列表（channel_category 表，平台级分类）
    Channel categories list
  operationId: channelCategories
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: integer }
                            name: { type: string }
                            icon: { type: string }
                            sort_order: { type: integer }
