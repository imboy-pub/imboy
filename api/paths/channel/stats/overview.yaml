# GET (channel) channelStats
get:
  tags: [user]
  summary: 频道整体统计
  description: |
    ## 中文 / English
    频道整体统计
    Channel overview stats
  operationId: channelStats
  parameters:
    - in: path
      name: channel_id
      required: true
      schema: { type: string }
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 参数非法 / 无权限 / 不存在
      content:
        application/json: { schema: { $ref: '../../../components/schemas/Envelope.yaml' } }
