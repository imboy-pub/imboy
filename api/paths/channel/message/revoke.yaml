# POST (channel) revokeChannelMessage
post:
  tags: [user]
  summary: 撤回自发消息
  description: |
    ## 中文 / English
    撤回自发消息
    Revoke own message
  operationId: revokeChannelMessage
  parameters:
    - in: path
      name: channel_id
      required: true
      schema: { type: string }
    - in: path
      name: message_id
      required: true
      schema: { type: string }
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 参数非法 / 无权限 / 不存在
      content:
        application/json: { schema: { $ref: '../../../components/schemas/Envelope.yaml' } }
