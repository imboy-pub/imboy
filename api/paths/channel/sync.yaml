# GET (channel) channelsSync
get:
  tags: [user]
  summary: 客户端增量同步
  description: |
    ## 中文 / English
    客户端增量同步
    Incremental sync
  operationId: channelsSync
  parameters:
    - in: query
      name: last_ts
      required: false
      schema: { type: integer, format: int64 }
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 参数非法 / 无权限 / 不存在
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
