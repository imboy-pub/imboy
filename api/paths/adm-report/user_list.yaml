# GET /adm/user/report/list — admin
get:
  tags: [admin]
  summary: admin adm-report - user_list
  description: |
    ## 中文 / English
    管理后台 adm-report handler 的 `user_list` 动作；路径 `/adm/user/report/list`。
    Admin handler `adm-report`, action `user_list`. Path `/adm/user/report/list`.
  operationId: adm_report_user_list
  parameters:
    - $ref: '../../components/parameters/PaginationPage.yaml'
    - $ref: '../../components/parameters/PaginationSize.yaml'
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
