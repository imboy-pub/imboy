# GET /adm/channel/report/list — admin
get:
  tags: [admin]
  summary: admin adm-report - channel_list
  description: |
    ## 中文 / English
    管理后台 adm-report handler 的 `channel_list` 动作；路径 `/adm/channel/report/list`。
    Admin handler `adm-report`, action `channel_list`. Path `/adm/channel/report/list`.
  operationId: adm_report_channel_list
  parameters:
    - $ref: '../../components/parameters/PaginationPage.yaml'
    - $ref: '../../components/parameters/PaginationSize.yaml'
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    description: 频道举报工单分页列表（target_type=channel）/ Paginated channel report ticket list
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: integer, format: int64 }
                            target_type: { type: string, description: '固定为 channel / Always "channel"' }
                            target_id: { type: integer, format: int64 }
                            reporter_uid: { type: integer, format: int64 }
                            reason: { type: string }
                            description: { type: string }
                            status: { type: integer }
                            handled_by: { type: integer, format: int64 }
                            handled_at: { type: string }
                            created_at: { type: string }
                            updated_at: { type: string }
                      total: { type: integer }
                      page: { type: integer }
                      size: { type: integer }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
