# POST /adm/moment/delete — admin
post:
  tags: [admin]
  summary: admin adm-moment - delete
  description: |
    ## 中文 / English
    管理后台 adm-moment handler 的 `delete` 动作；路径 `/adm/moment/delete`。
    Admin handler `adm-moment`, action `delete`. Path `/adm/moment/delete`.
  operationId: adm_moment_delete
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
