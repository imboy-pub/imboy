# POST /api/adm/moment/report/batch_resolve — admin
post:
  tags: [admin]
  summary: admin adm-moment - report_batch_resolve
  description: |
    ## 中文 / English
    管理后台 adm-moment handler 的 `report_batch_resolve` 动作；路径 `/adm/moment/report/batch_resolve`。
    Admin handler `adm-moment`, action `report_batch_resolve`. Path `/adm/moment/report/batch_resolve`.
  operationId: adm_moment_report_batch_resolve
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    description: 批量处理结果 / Batch resolve result
                    properties:
                      success_count:
                        type: integer
                        description: 成功处理条数 / Successfully resolved count
                      failed_count:
                        type: integer
                        description: 失败条数 / Failed count
                      failed_ids:
                        type: array
                        description: 失败的举报 ID 列表 / Failed report IDs
                        items:
                          type: string
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
