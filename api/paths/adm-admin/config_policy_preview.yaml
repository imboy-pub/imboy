# POST /adm/admin/config/policy/preview — admin
post:
  tags: [admin]
  summary: admin adm-admin - config_policy_preview
  description: |
    ## 中文 / English
    管理后台 adm-admin handler 的 `config_policy_preview` 动作；路径 `/adm/admin/config/policy/preview`。
    Admin handler `adm-admin`, action `config_policy_preview`. Path `/adm/admin/config/policy/preview`.
  operationId: adm_admin_config_policy_preview
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    additionalProperties: true
                    description: 预览后的有效策略对象
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
