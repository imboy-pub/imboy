# GET /api/adm/admin/config/policy/saved — admin
get:
  tags: [admin]
  summary: admin adm-admin - config_policy_saved
  description: |
    ## 中文 / English
    管理后台 adm-admin handler 的 `config_policy_saved` 动作；路径 `/adm/admin/config/policy/saved`。
    Admin handler `adm-admin`, action `config_policy_saved`. Path `/adm/admin/config/policy/saved`.
  operationId: adm_admin_config_policy_saved
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    additionalProperties: true
                    description: 已持久化的策略对象（saved_view 视图）
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
