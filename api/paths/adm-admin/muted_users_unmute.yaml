# POST /adm/admin/muted_users/unmute — admin
post:
  tags: [admin]
  summary: admin adm-admin - muted_users_unmute
  description: |
    ## 中文 / English
    管理后台 adm-admin handler 的 `muted_users_unmute` 动作；路径 `/adm/admin/muted_users/unmute`。
    Admin handler `adm-admin`, action `muted_users_unmute`. Path `/adm/admin/muted_users/unmute`.
  operationId: adm_admin_muted_users_unmute
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功（payload 为空对象）
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    description: 操作成功返回空对象 {}
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
