# POST /adm/admin/muted_users/unmute_batch — admin
post:
  tags: [admin]
  summary: admin adm-admin - muted_users_unmute_batch
  description: |
    ## 中文 / English
    管理后台 adm-admin handler 的 `muted_users_unmute_batch` 动作；路径 `/adm/admin/muted_users/unmute_batch`。
    Admin handler `adm-admin`, action `muted_users_unmute_batch`. Path `/adm/admin/muted_users/unmute_batch`.
  operationId: adm_admin_muted_users_unmute_batch
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    properties:
                      total:
                        type: integer
                        description: 提交的用户总数
                      success:
                        type: integer
                        description: 成功解除禁言数量
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
