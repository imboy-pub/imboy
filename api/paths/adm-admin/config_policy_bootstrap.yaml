# GET /adm/admin/config/policy/bootstrap — admin
get:
  tags: [admin]
  summary: admin adm-admin - config_policy_bootstrap
  description: |
    ## 中文 / English
    管理后台 adm-admin handler 的 `config_policy_bootstrap` 动作；路径 `/adm/admin/config/policy/bootstrap`。
    Admin handler `adm-admin`, action `config_policy_bootstrap`. Path `/adm/admin/config/policy/bootstrap`.
  operationId: adm_admin_config_policy_bootstrap
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    additionalProperties: true
                    description: 系统策略配置对象（admin_config_view 视图）
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
