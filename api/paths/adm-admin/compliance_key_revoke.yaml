# POST /adm/admin/compliance_key/revoke — admin
post:
  tags: [admin]
  summary: admin adm-admin - compliance_key_revoke
  description: |
    ## 中文 / English
    管理后台 adm-admin handler 的 `compliance_key_revoke` 动作；路径 `/adm/admin/compliance_key/revoke`。
    Admin handler `adm-admin`, action `compliance_key_revoke`. Path `/adm/admin/compliance_key/revoke`.
  operationId: adm_admin_compliance_key_revoke
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功（payload 为空对象）
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    description: 操作成功返回空对象 {}
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
