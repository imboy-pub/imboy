# ----------------------------------------------------------------------------
# imboy/api/paths/moment/like.yaml
# POST /v1/moment/{moment_id}/like — 点赞 / Like moment
# Handler: src/api/moment_handler.erl#like/2 → moment_logic:like_post/2
# ----------------------------------------------------------------------------
post:
  tags: [user]
  summary: 给动态点赞
  description: |
    ## 中文 / English
    给指定动态点赞。幂等。
    Like the moment. Idempotent.
  operationId: likeMoment
  parameters:
    - in: path
      name: moment_id
      required: true
      description: '动态 ID / Moment ID'
      schema: { type: string }
  responses:
    '200':
      description: 点赞成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    description: 无 payload，成功即点赞 / No payload on success
    '4XX':
      description: 动态不存在 / 无权访问
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
