# POST /adm/channel/{channel_id}/admins — admin
post:
  tags: [admin]
  summary: admin adm-channel - admins
  description: |
    ## 中文 / English
    管理后台 adm-channel handler 的 `admins` 动作；路径 `/adm/channel/{channel_id}/admins`。
    Admin handler `adm-channel`, action `admins`. Path `/adm/channel/{channel_id}/admins`.
  operationId: adm_channel_admins
  parameters:
    - in: path
      name: channel_id
      required: true
      schema: { type: string }
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
