# POST /adm/channel/{channel_id}/admin/{user_id} — admin
post:
  tags: [admin]
  summary: admin adm-channel - remove_admin
  description: |
    ## 中文 / English
    管理后台 adm-channel handler 的 `remove_admin` 动作；路径 `/adm/channel/{channel_id}/admin/{user_id}`。
    Admin handler `adm-channel`, action `remove_admin`. Path `/adm/channel/{channel_id}/admin/{user_id}`.
  operationId: adm_channel_remove_admin
  parameters:
    - in: path
      name: channel_id
      required: true
      schema: { type: string }
    - in: path
      name: user_id
      required: true
      schema: { type: string }
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
