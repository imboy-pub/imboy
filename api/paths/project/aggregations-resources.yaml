# GET /api/v1/projects/{project_id}/aggregations/resources — 四聚合之 resources（W2 ZC-06）
get:
  tags: [project]
  summary: "资源流（关联频道附件/文件类有界摘要）"
  description: |
    ## 中文
    资源流（关联频道附件/文件类有界摘要）。SQL 条数固定（无 N+1），返回有界摘要。
    ## English
    Bounded resource digest across linked channels. Fixed number of SQL statements (no N+1), bounded result.
  operationId: projectAggResources
  parameters:
    - name: project_id
      in: path
      required: true
      schema: { type: integer, format: int64 }
    - name: page
      in: query
      schema: { type: integer, default: 1 }
    - name: size
      in: query
      schema: { type: integer, default: 10 }
  responses:
    '200':
      description: 聚合摘要
      content:
        application/json:
          schema: { $ref: '../../components/schemas/Envelope.yaml' }
    '4XX':
      description: 无权限（403）/ 项目不存在（404）
      content:
        application/json:
          schema: { $ref: '../../components/schemas/Envelope.yaml' }
