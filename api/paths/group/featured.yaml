# GET (group) featuredGroups
get:
  tags: [user]
  summary: 精选群
  description: |
    ## 中文 / English
    运营精选公开群（is_featured=true）
    Featured public groups
  operationId: featuredGroups
  parameters:
    - in: query
      name: limit
      required: false
      description: '返回条数 / Max results'
      schema: { type: integer, default: 10 }
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    properties:
                      list: { type: array, items: { type: object } }
