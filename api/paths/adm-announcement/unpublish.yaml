# POST /adm/announcement/unpublish — admin
post:
  tags: [admin]
  summary: admin adm-announcement - unpublish
  description: |
    ## 中文 / English
    管理后台 adm-announcement handler 的 `unpublish` 动作；路径 `/adm/announcement/unpublish`。
    Admin handler `adm-announcement`, action `unpublish`. Path `/adm/announcement/unpublish`.
  operationId: adm_announcement_unpublish
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
