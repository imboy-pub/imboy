# POST /adm/announcement/delete — admin
post:
  tags: [admin]
  summary: admin adm-announcement - delete
  description: |
    ## 中文 / English
    管理后台 adm-announcement handler 的 `delete` 动作；路径 `/adm/announcement/delete`。
    Admin handler `adm-announcement`, action `delete`. Path `/adm/announcement/delete`.
  operationId: adm_announcement_delete
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required: [id]
          properties:
            id:
              type: string
              description: 公告 ID（TSID）/ Announcement ID (TSID)
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
