# GET /api/adm/group/notice/detail — admin
get:
  tags: [admin]
  summary: 管理后台群公告详情
  description: 获取指定群公告的详细信息（管理员视角）。Get group notice details (admin view).
  operationId: adm_group_notice_detail
  parameters:
    - in: query
      name: notice_id
      required: true
      schema: { type: string }
      description: 公告 ID
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../../components/schemas/Envelope.yaml'
              - type: object
                properties:
                  payload:
                    type: object
                    additionalProperties: true
                    description: '详情对象（字段以对应表/handler 为准）'
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
