# POST /adm/plugin/disable — admin
post:
  tags: [admin]
  summary: admin adm-plugin - disable
  description: |
    ## 中文 / English
    管理后台 adm-plugin handler 的 `disable` 动作；路径 `/adm/plugin/disable`。
    Admin handler `adm-plugin`, action `disable`. Path `/adm/plugin/disable`.
  operationId: adm_plugin_disable
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: true
  responses:
    '200':
      description: 成功
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
    '4XX':
      description: 无权限 / 参数非法
      content:
        application/json: { schema: { $ref: '../../components/schemas/Envelope.yaml' } }
